<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
                name="dataBindingExample"
                type="com.vchohan.androidexamples.DataBindingJavaObject" />
    </data>

    <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center">

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAlignment="center"
                android:textStyle="bold"
                android:onClick="@{dataBindingExample.onClickListener}"
                android:text="@{dataBindingExample.pageTitle}" />

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:onClick="@{dataBindingExample.onClickListener}"
                android:text="@{dataBindingExample.someText}" />

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{dataBindingExample.firstName}" />

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{dataBindingExample.lastName}" />

    </LinearLayout>

</layout>